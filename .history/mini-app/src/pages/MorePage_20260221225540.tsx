import type { FC } from 'react';

export const MorePage: FC = () => (
  <div style={{ padding: 16 }}>
    <h1 style={{ margin: 0, fontSize: 22 }}>More</h1>
    <p style={{ marginTop: 8, color: '#666' }}>Экран-заглушка (сетка)</p>
  </div>
);
